import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <ul>
    <li>1</li>
    <li>2</li>
    <li>3</li>
  </ul>,
  document.getElementById("root")
);

//just one default export per file
//more than one non-default export per file!!
//split up file in different file components
//modern version of require()
//import * = import everything as ...
